[project]
name = "browser-use-demo"
version = "0.1.0"
description = "企業情報自動収集スクリプト（browser-use）"
requires-python = ">=3.11"
dependencies = [
    "browser-use>=0.1.40",
    "langchain-ollama>=0.2.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
]

[tool.uv]
dev-dependencies = []
